name: Build Debug APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper/
          key: gradle-cache-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-cache-${{ runner.os }}-

      - name: Grant execute permission for Gradle wrapper
        run: chmod +x narayan662122-arch-Faluda-255bc12/ChessChatApp/ChessChatApp/gradlew

      - name: Build Debug APK
        working-directory: narayan662122-arch-Faluda-255bc12/ChessChatApp/ChessChatApp
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: narayan662122-arch-Faluda-255bc12/ChessChatApp/ChessChatApp/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
